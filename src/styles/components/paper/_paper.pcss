/* @define Paper */
.paper {
    @extend %align-col;
    @extend %align-items-center;
    @extend %justify-content-center;
    background: var(--white);
    border-radius: 4px;
    border-bottom: 0.25rem solid var(--active);
    min-height: 200px;
    min-width: 350px;
}
